cd /tmp 

USER="$@"
read -r -p "Are you sure you want to drop user: $USER? [y/N] " response
response=${response,,}    # tolower
if [[ $response =~ ^(yes|y)$ ]]
then
    output=`sudo -u postgres psql postgres -tAc "DROP USER IF EXISTS $USER
"`
fi
