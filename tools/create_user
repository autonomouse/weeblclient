cd /tmp

output=`sudo -u postgres psql postgres -tAc "SELECT 1 FROM pg_roles WHERE rolname='weebl'" 2> /dev/null`

if [ "$output" != 1 ]
then
	sudo -u postgres createuser weebl
else
	echo "User 'weebl' exists already."
fi
