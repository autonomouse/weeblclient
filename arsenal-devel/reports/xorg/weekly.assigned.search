# Bugs assigned to someone this past week

source.teams = ubuntu-x-swat

require.max_days_since_modified = 7
# TODO: order_by date_assigned might be nice?
require.order_by = date_last_message

exclude.bug_task.max_days_since.date_assigned = 7

quick = True
