# Retracer crash bugs filed this week with at least one duplicate

source.teams = ubuntu-x-swat

require.tags = apport-crash, ${current_ubuntu_development_codename}
require.sort_by = number_of_duplicates
require.max_days_since_created = 7

exclude.bug.less_than.number_of_duplicates = 1

quick = True
